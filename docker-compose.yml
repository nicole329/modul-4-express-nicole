version: "3.9"

services:
  db:
    image: postgres
    container_name: projekt-test-db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: syntax_db
    ports:
      - "5432:5432"